
extends layouts/main-layout.pug

block content 
    main.main-content 
        form(action="/admin/product", method="POST", id="add-product-form")
            input.input(type="text", name="title", id="title", placeholder="Product name")
            button.submit(type="submit") Add Product
        //-     p#error-msg

        //- script.
        //-     (function(){
        //-         var form = document.getElementById('add-product-form');
        //-         var input = document.getElementById('title');
        //-         var err = document.getElementById('error-msg');
        //-         if(!form) return;
        //-         form.addEventListener('submit', function(e){
        //-             if(!input.value || input.value.trim() === ''){
        //-                 e.preventDefault();
        //-                 err.textContent = 'Please write a product name.';
        //-                 err.style.color = 'red';
        //-                 input.focus();
        //-             } else {
        //-                 err.textContent = '';
        //-             }
        //-         });
        //-         input.addEventListener('input', function(){
        //-             if(input.value && input.value.trim() !== '') err.textContent = '';
        //-         });
        //-     })();